{{-- <x-layout>
    <div
        class="w-full mt-6 max-w-screen-xl flex flex-wrap flex-col items-center justify-between mx-auto md:flex-row px-6">
        <h1 class="text-3xl font-bold mb-6">Pilih Kategori</h1>
        <ul class="w-full md:w-1/2">
            @foreach ($categories as $category)
                <li class="mb-4">
                    <a href="{{ route('category.videos', $category->id) }}"
                        class="text-2xl text-blue-500 hover:underline">
                        {{ $category->name }}
                    </a>
                </li>
            @endforeach
        </ul>
    </div>
</x-layout> --}}
